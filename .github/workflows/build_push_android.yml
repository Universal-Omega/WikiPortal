name: Artifact Build

on: [push]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7

    - name: Set up JDK 21
      uses: actions/setup-java@v5.7.0
      with:
        distribution: temurin
        java-version: 21

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v5
      with:
        cache-encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
        cache-read-only: false

    - name: Run Gradle
      run: ./gradlew :androidApp:assembleAlphaDebug :composeApp:detekt

    - name: Test
      if: always()
      run: cat composeApp/build/generated/sqldelight/code/WikiPortalDatabase/commonMain/org/wikitide/wikiportal/db/WikiPortalQueries.kt

    - name: Upload Artifact
      uses: actions/upload-artifact@v7
      with:
        name: debug-build
        path: "androidApp/build/outputs/apk/alpha/debug/*.apk"
